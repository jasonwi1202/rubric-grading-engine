/**
 * Dashboard layout — wraps all authenticated teacher views.
 *
 * This is a stub. A future milestone will add:
 * - Session guard (redirect to /login if unauthenticated)
 * - Persistent sidebar and top navigation
 */
export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
