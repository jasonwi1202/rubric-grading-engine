/**
 * Application-wide constants.
 *
 * Use these instead of hardcoded strings so names and copy can be updated
 * from a single location.
 */

/** The product's display name — used in nav, footer, page titles, and copy. */
export const PRODUCT_NAME = "GradeWise";

/** General contact / support email address. */
export const SUPPORT_EMAIL = "hello@gradewise.app";
