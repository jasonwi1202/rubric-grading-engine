# Gitleaks configuration
# https://github.com/gitleaks/gitleaks#configuration

title = "rubric-grading-engine gitleaks config"

[extend]
# Extend the default ruleset
useDefault = true

[allowlist]
description = "Ignore vendored dependencies and gitignored local files"
paths = [
    # Python virtual environment — gitignored, never committed
    '''.venv/''',
    # Local secrets file — gitignored, never committed
    '''\.env$''',
    # Next.js build output — gitignored, never committed
    '''frontend/\.next/''',
]
regexes = [
    # Commented-out optional API key placeholder in .env.example — not a real secret
    '''#\s*INTEGRITY_API_KEY=''',
    # Similarity threshold config value — not a secret
    '''INTEGRITY_SIMILARITY_THRESHOLD=''',
    # smoke_test_demo.py uses body_contains= as a Check constructor kwarg — not a key
    '''body_contains=''',
]
