"""Migrations sub-package.

This package contains the Alembic environment (``env.py``), the migration
file template (``script.py.mako``), and generated version files under
``versions/``.

Do not import application code from within migration files — use raw SQL
or ``op.*`` helpers only, as described in ``docs/architecture/migrations.md``.
"""
